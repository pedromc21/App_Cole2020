@model IEnumerable<App_Cole2020.Common.Models.Period>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ciclo_Escolar)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ciclo_Escolar)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Periodo_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Periodo_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Periodo_Id })
        </td>
    </tr>
}

</table>
